import 'package:flutter/material.dart';

class KeoBuaBaoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Cờ Caro")),
      body: Center(child: Text("Welcome to Kéo búa bao!")),
    );
  }
}
