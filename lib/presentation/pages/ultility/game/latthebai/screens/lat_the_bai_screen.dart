import 'package:flutter/material.dart';

class LatTheBaiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Cờ Caro")),
      body: Center(child: Text("Welcome to Lật thẻ bài!")),
    );
  }
}
